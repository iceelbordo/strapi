{
  "collectionName": "components_video_hosting_service_videos",
  "info": {
    "name": "video",
    "icon": "film"
  },
  "options": {},
  "attributes": {
    "provider": {
      "type": "enumeration",
      "enum": [
        "Vimeo"
      ],
      "required": true,
      "default": "Vimeo"
    },
    "embedID": {
      "type": "string",
      "required": true,
      "maxLength": 255,
      "regex": "^[0-9]+$"
    }
  }
}
